[project]
name = "sift"
version = "0.1.0"
description = "Weekly AI-news curation pipeline: fetch RSS feeds, dedup, rank with one Claude call, render a digest."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
dependencies = [
    "feedparser>=6.0",
    "httpx>=0.27",
    "anthropic>=0.40",
    "markdown>=3.10.2",
    # Pillow renders the static-raster OG / per-issue cover cards. Card
    # generation is best-effort (sift.card), so a missing Pillow never breaks a
    # run — but the weekly run needs it to produce per-issue share images.
    "pillow>=12.3.0",
]

[project.scripts]
sift = "sift.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=7.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sift"]
